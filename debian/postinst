#!/bin/sh -e

case "$1" in
  configure)
    # detect devices
    /usr/bin/detect-alsa-devices

    # temlate files
    /usr/bin/process-template --owner root --group root --mode 0644 /etc/asound.conf

  ;;

esac

exit 0
